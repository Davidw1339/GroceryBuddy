matrix:
  include:
  - language: python
    python:
      - "3.7-dev"
    before_install:
      - cd backend
    install:
      - pip install -r requirements.txt
    script:
      - pytest

  - language: node_js
    node_js:
      - "8"
    cache:
      directories:
        - frontend/node_modules
    before_install:
      - cd frontend
    install:
      - npm install
    script:
      - npm run test

